# Required: this app mounts at /chat-app under the dashboard zone.
# The value must match the dashboard's rewrite source for this app.
NEXT_PUBLIC_BASE_PATH=/apps/chat-app

# Mesh API (server-only)
MESH_API_KEY=your_mesh_api_key_here
MESH_API_URL=https://api.meshapi.ai/v1
